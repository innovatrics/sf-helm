apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: {{ .Values.extractor.name | quote }}
  labels:
    app: {{ .Values.extractor.name | quote }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.extractor.name | quote }}
  template:
    metadata:
      labels:
        app: {{ .Values.extractor.name | quote }}
    spec:
      serviceAccountName: {{ .Values.serviceAccount.name | quote }}
      automountServiceAccountToken: {{ .Values.serviceAccount.automountServiceAccountToken }}
      topologySpreadConstraints:
        {{- include "smartface.topologySpread" (dict "appLabel" .Values.extractor.name) | nindent 8 }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: {{ .Values.extractor.name | quote }}
        image: {{ include "smartface.image" (dict "local" .Values.extractor.image "global" .Values.global.image "defaultVersion" .Chart.AppVersion) }}
        imagePullPolicy: {{ .Values.extractor.image.pullPolicy }}
        env:
        {{- include "smartface.commonEnv" . | nindent 8 }}
        {{- include "smartface.rmqConfig" . | nindent 8 }}
        resources:
          {{- toYaml .Values.extractor.resources | nindent 10 }}
        volumeMounts:
        {{- include "smartface.licVolumeMount" . | nindent 8 }}
      volumes:
        {{- include "smartface.licVolume" . | nindent 8 }}
      {{- with .Values.extractor.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.extractor.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
