apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: {{ .Values.detector.name | quote }}
  labels:
    app: {{ .Values.detector.name | quote }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.detector.name | quote }}
  template:
    metadata:
      labels:
        app: {{ .Values.detector.name | quote }}
    spec:
      serviceAccountName: {{ .Values.serviceAccount.name | quote }}
      automountServiceAccountToken: {{ .Values.serviceAccount.automountServiceAccountToken }}
      topologySpreadConstraints:
        {{- include "smartface.topologySpread" (dict "appLabel" .Values.detector.name) | nindent 8 }}
      imagePullSecrets:
      - name: {{ .Values.image.secretName | quote }}
      containers:
      - name: {{ .Values.detector.name | quote }}
        image: "{{ .Values.image.registry }}sf-detector:{{ .Chart.AppVersion }}"
        env:
        {{- include "smartface.commonEnv" . | nindent 8 }}
        {{- include "smartface.rmqConfig" . | nindent 8 }}
        resources:
          {{- toYaml .Values.detector.resources | nindent 10 }}
        volumeMounts:
        {{- include "smartface.licVolumeMount" . | nindent 8 }}
      volumes:
        {{- include "smartface.licVolume" . | nindent 8 }}
