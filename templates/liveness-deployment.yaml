apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: {{ .Values.liveness.name | quote }}
  labels:
    app: {{ .Values.liveness.name | quote }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.liveness.name | quote }}
  template:
    metadata:
      labels:
        app: {{ .Values.liveness.name | quote }}
    spec:
      serviceAccountName: {{ .Values.serviceAccount.name | quote }}
      automountServiceAccountToken: {{ .Values.serviceAccount.automountServiceAccountToken }}
      topologySpreadConstraints:
        {{- include "smartface.topologySpread" (dict "appLabel" .Values.liveness.name) | nindent 8 }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: {{ .Values.liveness.name | quote }}
        image: {{ include "smartface.image" (dict "local" .Values.liveness.image "global" .Values.global.image "defaultVersion" .Chart.AppVersion) }}
        imagePullPolicy: {{ .Values.liveness.image.pullPolicy }}
        env:
        {{- include "smartface.commonEnv" . | nindent 8 }}
        {{- include "smartface.rmqConfig" . | nindent 8 }}
        resources:
          {{- toYaml .Values.liveness.resources | nindent 10 }}
        volumeMounts:
        {{- include "smartface.licVolumeMount" . | nindent 8 }}
      volumes:
        {{- include "smartface.licVolume" . | nindent 8 }}
      {{- with .Values.liveness.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.liveness.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
