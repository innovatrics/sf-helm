{{- if .Values.authApi.enabled }}
apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: {{ .Values.authApi.name | quote }}
  labels:
    app: {{ .Values.authApi.name | quote }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.authApi.name | quote }}
  template:
    metadata:
      labels:
        app: {{ .Values.authApi.name | quote }}
    spec:
      serviceAccountName: {{ .Values.serviceAccount.name | quote }}
      automountServiceAccountToken: {{ .Values.serviceAccount.automountServiceAccountToken }}
      topologySpreadConstraints:
        {{- include "sf-cloud-matcher.topologySpread" (dict "appLabel" .Values.authApi.name) | nindent 8 }}
      imagePullSecrets:
      - name: {{ .Values.image.secretName | quote }}
      containers:
      - name: {{ .Values.authApi.name | quote }}
        image: "{{ .Values.image.registry }}sf-api:{{ .Chart.AppVersion }}"
        ports:
        - name: "http"
          containerPort: {{ .Values.authApi.containerPort }}
        - name: {{ .Values.metrics.portName | quote }}
          containerPort: {{ .Values.metrics.port }}
        env:
        {{- include "sf-cloud-matcher.authenticationConfig" . | nindent 8 }}
        {{- include "sf-cloud-matcher.commonEnv" . | nindent 8 }}
        {{- include "sf-cloud-matcher.dbConfig" . | nindent 8 }}
        {{- include "sf-cloud-matcher.s3Config" . | nindent 8 }}
        {{- include "sf-cloud-matcher.rmqConfig" . | nindent 8 }}
        {{- include "sf-cloud-matcher.apiFeaturesConfig" . | nindent 8 }}
        {{- include "sf-cloud-matcher.statisticsPublish" . | nindent 8 }}
        - name: "Multitenancy__UseMultitenancy"
          value: {{ .Values.multitenancy.enabled | quote }}
        - name: "Hosting__Host"
          value: "http://0.0.0.0"
        - name: "Hosting__Port"
          value: {{ .Values.authApi.containerPort | quote }}
        resources:
          {{- toYaml .Values.authApi.resources | nindent 10 }}
        volumeMounts:
        {{- include "sf-cloud-matcher.licVolumeMount" . | nindent 8 }}
      {{- if and .Values.migration.enabled .Values.authApi.initMigration }}
      initContainers:
      {{- include "sf-cloud-matcher.migrationInitContainer" . | nindent 6 }}
      {{- end }}
      volumes:
        {{- include "sf-cloud-matcher.licVolume" . | nindent 8 }}
{{- end }}
